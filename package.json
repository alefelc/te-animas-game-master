{
  "name": "te-animas-game-master",
  "version": "3.0.1",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.12"
  },
  "scripts": {
    "build:contracts": "npm run build --prefix contracts",
    "build": "npm run build:contracts && tsc -p tsconfig.json",
    "start": "node dist/server.js"
  },
  "dependencies": {
    "@te-animas/contracts": "file:./contracts",
    "openai": "^6.0.0",
    "zod": "4.4.3"
  },
  "devDependencies": {
    "@types/node": "24.10.13",
    "typescript": "6.0.3"
  }
}
